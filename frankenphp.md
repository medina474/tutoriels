
```shell
apt install --no-install-recommends curl
curl -fsSL https://files.henderkes.com/install.sh | sh -s 8.4
apt install frankenphp
```

```shell
apt install unzip php-zts-zip
```

```shell
apt install php-zts-pdo php-zts-pdo-pgsql
```

apt install --no-install-recommends git


mv composer.phar /usr/local/bin/composer
